from distutils.core import setup

setup(
    name = "predictive-text-llm",
    version = "0.1",
    description = "Simple console application that uses an LLM as a predictive text tool",
    author = "Matthew Walder",
    packages=["predictive-text-llm"]
)